# Write your MySQL query statement below
SELECT sell_date, COUNT(product) num_sold, GROUP_CONCAT(DISTINCT product) products

FROM (
    SELECT sell_date, product
    FROM Activities
    GROUP BY sell_date, product
    ) alias


GROUP BY sell_date
ORDER BY sell_date, product